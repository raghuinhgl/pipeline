pipeline {
  agent none
  stages {
    stage ('build') {
    agent { label 'master' }
       steps {
          sh '''
                  echo "this is build stage"
                  sleep 2
             '''
             }
    }
    stage ('deploy') {
    aget { label 'javacod-s1' }
       steps {
          sh '''
                  echo "this is deploy stage"
                  sleep 2
             '''
             }
    }
    stage ('test') {
    agent { label 'master' }
       steps {
          sh '''
                  echo "this is test stage"
                  sleep 2
             '''
             }
    }
  }
}
